# Formula A+B+C Football Web App

Mobile-upload version: all web UI is embedded in `app.py`; no subfolders are required.

Railway start command: `uvicorn app:app --host 0.0.0.0 --port $PORT`

Set `OPENAI_API_KEY` in Railway variables to enable live automatic research.
